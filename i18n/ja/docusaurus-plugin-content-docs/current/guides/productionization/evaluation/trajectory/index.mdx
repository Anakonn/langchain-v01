---
sidebar_position: 4
translated: true
---

# トラジェクトリ評価器

LangChainのトラジェクトリ評価器は、エージェントを評価するためのより包括的なアプローチを提供します。これらの評価器は、エージェンが取った一連のアクションとその対応する応答、つまり「トラジェクトリ」を評価します。これにより、エージェントの有効性と能力をより適切に測定できます。

トラジェクトリ評価器は `AgentTrajectoryEvaluator` インターフェースを実装し、2つの主要なメソッドが必要です:

- `evaluate_agent_trajectory`: このメソッドは同期的にエージェントのトラジェクトリを評価します。
- `aevaluate_agent_trajectory`: この非同期のカウンターパートにより、効率的に並列で評価を実行できます。

両方のメソッドは3つの主要なパラメーターを受け取ります:

- `input`: エージェントに与えられた初期入力。
- `prediction`: エージェントの最終的な予測応答。
- `agent_trajectory`: エージェントが取った中間ステップ。これはタプルのリストで表されます。

これらのメソッドは辞書を返します。カスタム実装では、`score`(エージェントの有効性を示す浮動小数点数)と`reasoning`(スコアの理由を説明する文字列)を返すことをお勧めします。

`return_intermediate_steps=True` パラメーターでエージェントを初期化すると、特別なコールバックに頼ることなく、すべての中間ステップを収集できます。

トラジェクトリ評価器の実装と使用の詳細については、以下のセクションを参照してください。

import DocCardList from "@theme/DocCardList";

<DocCardList />
