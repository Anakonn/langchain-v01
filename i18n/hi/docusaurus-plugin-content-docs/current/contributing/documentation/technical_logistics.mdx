---
translated: true
---

# तकनीकी लॉजिस्टिक्स

LangChain दस्तावेज़ीकरण दो घटकों से मिलकर बना है:

1. मुख्य दस्तावेज़ीकरण: [python.langchain.com](https://python.langchain.com/) पर होस्ट किया जाता है, यह व्यापक संसाधन प्राथमिक उपयोगकर्ता-उन्मुख दस्तावेज़ीकरण के रूप में कार्य करता है। यह व्यापक विषयों को कवर करता है, जिनमें ट्यूटोरियल, उपयोग मामले, एकीकरण और अधिक शामिल हैं, जो LangChain के साथ बनाने में व्यापक मार्गदर्शन प्रदान करता है। इस दस्तावेज़ीकरण के लिए सामग्री मोनोरेपो के `/docs` निर्देशिका में मौजूद है।
2. कोड में दस्तावेज़ीकरण: यह खुद कोडबेस का दस्तावेज़ीकरण है, जिसका उपयोग बाहरी रूप से [API संदर्भ](https://api.python.langchain.com/en/latest/langchain_api_reference.html) को जनरेट करने के लिए भी किया जाता है। API संदर्भ के लिए सामग्री कोडबेस में डॉकस्ट्रिंग को स्कैन करके ऑटो-जनरेट की जाती है। इस कारण से हम विकासकर्ताओं से अनुरोध करते हैं कि वे अपने कोड का अच्छी तरह से दस्तावेज़ीकरण करें।

मुख्य दस्तावेज़ीकरण [Quarto](https://quarto.org) और [Docusaurus 2](https://docusaurus.io/) का उपयोग करके बनाया जाता है।

`API संदर्भ` को बड़े पैमाने पर [sphinx](https://www.sphinx-doc.org/en/master/) द्वारा कोड से ऑटो-जनरेट किया जाता है और [Read the Docs](https://readthedocs.org/) द्वारा होस्ट किया जाता है।

हम दस्तावेज़ीकरण में किए गए सभी योगदानों का सम्मान करते हैं, चाहे वह टाइपो को ठीक करना हो, नया ट्यूटोरियल या उदाहरण जोड़ना हो और चाहे वह मुख्य दस्तावेज़ीकरण में हो या API संदर्भ में।

लिंटिंग की तरह, हम मानते हैं कि दस्तावेज़ीकरण परेशान कर सकता है। यदि आप इसे नहीं करना चाहते हैं, तो कृपया परियोजना के मेंटेनर से संपर्क करें, और वे इसमें आपकी मदद कर सकते हैं। हम नहीं चाहते कि यह अच्छे कोड के योगदान के लिए एक रोक हो।

## 📜 मुख्य दस्तावेज़ीकरण

मुख्य दस्तावेज़ीकरण की सामग्री मोनोरेपो के `/docs` निर्देशिका में स्थित है।

दस्तावेज़ीकरण का लेखन ipython notebooks (`.ipynb` फ़ाइलें) और मार्कडाउन (`.mdx` फ़ाइलें) का उपयोग करके किया जाता है। नोटबुक को [Quarto](https://quarto.org) का उपयोग करके मार्कडाउन में परिवर्तित किया जाता है और फिर [Docusaurus 2](https://docusaurus.io/) का उपयोग करके बनाया जाता है।

मुख्य दस्तावेज़ीकरण में योगदान करने के लिए स्वतंत्र महसूस करें! 🥰

दस्तावेज़ीकरण में परिवर्तन करने के बाद:

1. दस्तावेज़ीकरण अच्छी तरह से प्रारूपित और त्रुटि-मुक्त है, इसे सुनिश्चित करने के लिए लिंटिंग और प्रारूपण कमांड चलाएं (नीचे देखें)।
2. वैकल्पिक रूप से दस्तावेज़ीकरण को स्थानीय रूप से बनाएं ताकि परिवर्तनों को सत्यापित किया जा सके।
3. परिवर्तनों के साथ एक पुल अनुरोध बनाएं।
4. आप पुल अनुरोध `Conversation` पृष्ठ पर `View deployment` या `Visit Preview` बटन पर क्लिक करके दस्तावेज़ीकरण परिवर्तनों की पूर्वावलोकन देख सकते हैं।

## ⚒️ स्थानीय रूप से दस्तावेज़ीकरण लिंट और बिल्ड करना

दस्तावेज़ीकरण लिखने के बाद, आप स्थानीय रूप से इसे लिंट और बिल्ड करना चाह सकते हैं ताकि यह अच्छा दिखे और त्रुटि-मुक्त हो।

यदि आप इसे स्थानीय रूप से बना नहीं सकते हैं, तो भी ठीक है, क्योंकि आप पुल अनुरोध पृष्ठ पर दस्तावेज़ीकरण का पूर्वावलोकन देख सकेंगे।

### निर्भरताएं स्थापित करें

- [Quarto](https://quarto.org) - Jupyter notebooks (`.ipynb` फ़ाइलें) को Docusaurus में सर्व करने के लिए mdx फ़ाइलों में परिवर्तित करने वाला पैकेज। [डाउनलोड लिंक](https://quarto.org/docs/download/)।

**मोनोरेपो रूट** से, निम्नलिखित कमांड चलाकर निर्भरताएं स्थापित करें:

```bash
poetry install --with lint,docs --no-root
```

### बिल्डिंग

दस्तावेज़ीकरण को बनाने वाला कोड मोनोरेपो के `/docs` निर्देशिका में स्थित है।

निम्नलिखित कमांडों में, `api_` उपसर्ग इंगित करता है कि वे API संदर्भ के लिए हैं।

दस्तावेज़ीकरण को बनाने से पहले, बिल्ड निर्देशिका को हमेशा साफ करना एक अच्छा विचार है:

```bash
make docs_clean
make api_docs_clean
```

अगला, आप नीचे दिए गए अनुसार दस्तावेज़ीकरण को बना सकते हैं:

```bash
make docs_build
make api_docs_build
```

अंत में, सभी लिंक मान्य हैं, इसे सुनिश्चित करने के लिए लिंक चेकर चलाएं:

```bash
make docs_linkcheck
make api_docs_linkcheck
```

### लिंटिंग और प्रारूपण

मुख्य दस्तावेज़ीकरण को **मोनोरेपो रूट** से लिंट किया जाता है। मुख्य दस्तावेज़ीकरण को लिंट करने के लिए, वहाँ से निम्नलिखित चलाएं:

```bash
make lint
```

यदि आपके पास प्रारूपण से संबंधित त्रुटियां हैं, तो आप निम्नलिखित से उन्हें स्वचालित रूप से ठीक कर सकते हैं:

```bash
make format
```

## ⌨️ कोड में दस्तावेज़ीकरण

कोड में दस्तावेज़ीकरण को बड़े पैमाने पर [sphinx](https://www.sphinx-doc.org/en/master/) द्वारा कोड से ऑटो-जनरेट किया जाता है और [Read the Docs](https://readthedocs.org/) द्वारा होस्ट किया जाता है।

API संदर्भ उपयोगी हो, इसके लिए कोडबेस को अच्छी तरह से दस्तावेज़ीकृत होना चाहिए। इसका मतलब है कि सभी फ़ंक्शन, क्लास और मेथड के पास एक डॉकस्ट्रिंग होना चाहिए जो बताता है कि वे क्या करते हैं, तर्क क्या हैं, और रिटर्न मान क्या है। यह सामान्य में एक अच्छी प्रथा है, लेकिन LangChain के लिए यह विशेष रूप से महत्वपूर्ण है क्योंकि API संदर्भ कोडबेस का उपयोग करने के लिए डेवलपर्स के लिए प्राथमिक संसाधन है।

हम आमतौर पर डॉकस्ट्रिंग के लिए [Google Python Style Guide](https://google.github.io/styleguide/pyguide.html#38-comments-and-docstrings) का पालन करते हैं।

एक अच्छी तरह से दस्तावेज़ीकृत फ़ंक्शन का एक उदाहरण यहां दिया गया है:

```python

def my_function(arg1: int, arg2: str) -> float:
    """This is a short description of the function. (It should be a single sentence.)

    This is a longer description of the function. It should explain what
    the function does, what the arguments are, and what the return value is.
    It should wrap at 88 characters.

    Examples:
        This is a section for examples of how to use the function.

        .. code-block:: python

            my_function(1, "hello")

    Args:
        arg1: This is a description of arg1. We do not need to specify the type since
            it is already specified in the function signature.
        arg2: This is a description of arg2.

    Returns:
        This is a description of the return value.
    """
    return 3.14
```

### लिंटिंग और प्रारूपण

कोड में दस्तावेज़ीकरण को दस्तावेज़ीकृत पैकेजों से संबंधित निर्देशिकाओं से लिंट किया जाता है।

उदाहरण के लिए, यदि आप `langchain-community` पैकेज पर काम कर रहे हैं, तो आप `langchain-community` निर्देशिका में कार्य करेंगे:

```bash
cd [root]/libs/langchain-community
```

यदि आपने ऐसा नहीं किया है, तो पैकेज के लिए एक वर्चुअल पर्यावरण सेट अप करें।

पैकेज के लिए निर्भरताएं स्थापित करें।

```bash
poetry install --with lint
```

फिर आप कोड में दस्तावेज़ीकरण को लिंट और प्रारूपित करने के लिए निम्नलिखित कमांड चला सकते हैं:

```bash
make format
make lint
```

## प्रलेखन परिवर्तनों की पुष्टि करें

रिपॉजिटरी में प्रलेखन परिवर्तनों को धकेलने के बाद, आप प्रीव्यू और सत्यापित कर सकते हैं कि परिवर्तन वह हैं जो आप चाहते थे, इसके लिए पुल अनुरोध `Conversation` पृष्ठ पर `View deployment` या `Visit Preview` बटन पर क्लिक करें।
यह आपको प्रलेखन परिवर्तनों के प्रीव्यू पर ले जाएगा।
यह प्रीव्यू [Vercel](https://vercel.com/docs/getting-started-with-vercel) द्वारा बनाया गया है।
