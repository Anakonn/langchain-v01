---
translated: true
---

# TigerGraph

>[TigerGraph](https://www.tigergraph.com/tigergraph-db/)은 기본적으로 분산되어 있고 고성능 그래프 데이터베이스입니다.
> 정점과 엣지로 구성된 그래프 형식으로 데이터를 저장하면 풍부한 관계가 생성되어,
> LLM 응답을 기반으로 하는 데 이상적입니다.

`TigerGraph`와 `LangChain` 통합의 큰 예시는 [여기](https://github.com/tigergraph/graph-ml-notebooks/blob/main/applications/large_language_models/TigerGraph_LangChain_Demo.ipynb)에 제시되어 있습니다.

## 설치 및 설정

`TigerGraph` 데이터베이스에 연결하는 방법은 [여기](https://docs.tigergraph.com/pytigergraph/current/getting-started/connection)의 지침을 따르세요.

Python SDK를 설치하세요:

```bash
pip install pyTigerGraph
```

## 예시

`TigerGraph InquiryAI` 기능을 활용하려면 `langchain_community.graphs`에서 `TigerGraph`를 가져올 수 있습니다.

```python
<!--IMPORTS:[{"imported": "TigerGraph", "source": "langchain_community.graphs", "docs": "https://api.python.langchain.com/en/latest/graphs/langchain_community.graphs.tigergraph_graph.TigerGraph.html", "title": "TigerGraph"}]-->
import pyTigerGraph as tg

conn = tg.TigerGraphConnection(host="DATABASE_HOST_HERE", graphname="GRAPH_NAME_HERE", username="USERNAME_HERE", password="PASSWORD_HERE")

### ==== CONFIGURE INQUIRYAI HOST ====
conn.ai.configureInquiryAIHost("INQUIRYAI_HOST_HERE")

from langchain_community.graphs import TigerGraph

graph = TigerGraph(conn)
result = graph.query("How many servers are there?")
print(result)
```
