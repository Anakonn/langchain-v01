---
sidebar_position: 4
translated: true
---

# 트래젝토리 평가기

LangChain의 트래젝토리 평가기는 에이전트를 평가하는 더 포괄적인 접근 방식을 제공합니다. 이러한 평가기는 에이전트가 취한 전체 액션 시퀀스와 그에 따른 응답을 평가하며, 이를 "트래젝토리"라고 합니다. 이를 통해 에이전트의 효과성과 능력을 더 잘 측정할 수 있습니다.

트래젝토리 평가기는 `AgentTrajectoryEvaluator` 인터페이스를 구현하며, 두 가지 주요 메서드를 요구합니다:

- `evaluate_agent_trajectory`: 에이전트의 트래젝토리를 동기적으로 평가하는 메서드입니다.
- `aevaluate_agent_trajectory`: 효율성을 위해 병렬로 평가를 실행할 수 있는 비동기 메서드입니다.

두 메서드는 세 가지 주요 매개변수를 수락합니다:

- `input`: 에이전트에게 제공된 초기 입력입니다.
- `prediction`: 에이전트의 최종 예측 응답입니다.
- `agent_trajectory`: 에이전트가 취한 중간 단계들을 튜플 리스트로 나타낸 것입니다.

이 메서드들은 사전을 반환합니다. 커스텀 구현에서는 에이전트의 효과를 나타내는 `score`(부동 소수점)와 점수의 이유를 설명하는 `reasoning`(문자열)을 반환하는 것이 권장됩니다.

에이전트를 `return_intermediate_steps=True` 매개변수로 초기화하여 에이전트의 트래젝토리를 캡처할 수 있습니다. 이를 통해 특별한 콜백에 의존하지 않고 모든 중간 단계를 수집할 수 있습니다.

트래젝토리 평가기의 구현 및 사용에 대한 자세한 내용은 아래 섹션을 참조하십시오.

import DocCardList from "@theme/DocCardList";

<DocCardList />